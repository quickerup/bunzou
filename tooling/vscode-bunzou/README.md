# vscode-bunzou — not built

Syntax highlighting for .bunzou files. Low effort, high goodwill with
early adopters — worth doing early per the phase plan, independent of
compiler internals since it only needs the lexer's token categories
(compiler/src/frontend/lexer.ts), not a working checker or codegen.

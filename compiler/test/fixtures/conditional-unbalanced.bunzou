state ToggleState {
    On
}

behavior On {
    on Flip(msg) -> On {
        if (msg.approved) {
            consume(self);
            return On;
        } else {
            return self;
        }
    }
}
